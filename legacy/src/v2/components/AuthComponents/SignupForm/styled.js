import styled from "styled-components";

export const Container = styled.div`
  button {
    margin: ${16 / 16}rem auto;
  }
`;

export const FormTitle = styled.h1`
  width: 100%;
  text-align: center;
  font-size: ${24 / 16}rem;
  margin: ${24 / 16}rem auto;
  letter-spacing: ${4 / 16}rem;
  text-transform: capitalize;
`;
